<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>All Songs</h1>
    <ul>
      <%= @songs.each do |song| %>
        <li><%= link_to song.artist.name + ' - ' + song.title, song_path(song.id) %></li>
      <% end %>
    </ul>
  </body>
</html>
